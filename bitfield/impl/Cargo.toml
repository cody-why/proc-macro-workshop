[package]
name = "bitfield-impl"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
proc-macro = true

[dependencies]
syn = {version = "1", features = ["full","extra-traits","visit-mut"]}
quote = "1.0"
proc-macro2 = "1.0"
proc-macro-error = "1"

bits = {path = "../bits"}
